server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG

#다국어 파일 설정
spring:
  messages:
    basename: messages # 우리가 사용할 message 파일의 이름
  # SWAGGER 설정
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher